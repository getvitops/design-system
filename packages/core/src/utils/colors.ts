/**
 * Moved to @getvitops/utils/color (the shared, isomorphic color engine used by
 * both the generator and these web components). Re-exported here so existing
 * relative imports keep working.
 */
export * from '@getvitops/utils/color';
