/**
 * @getvitops/astro — Astro integration for the Vitops design system.
 *
 * For now this exposes the HTML/type helpers used when authoring Astro components
 * (parsing rendered slots, building markup, style lists, element type guards).
 * Astro components that wrap @getvitops/core's web components will live here too.
 */
export * from './html.ts';
export * from './types.ts';
