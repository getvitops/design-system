---
import { itemListGraph } from '@getvitops/utils';
import type { ItemListEntry, ItemListGraphOptions } from '@getvitops/utils';

export type { ItemListEntry };
export type CarouselItem = ItemListEntry;
export type Props = ItemListGraphOptions;

const jsonLd = itemListGraph(Astro.props);
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
<slot />
