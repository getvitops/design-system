---
import { localBusinessGraph } from '@getvitops/utils';
import type { LocalBusinessGraphOptions } from '@getvitops/utils';

export type Props = LocalBusinessGraphOptions;

const jsonLd = localBusinessGraph(Astro.props);
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
<slot />
