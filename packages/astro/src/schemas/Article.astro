---
import { articleGraph } from '@getvitops/utils';
import type { ArticleGraphOptions } from '@getvitops/utils';

export type Props = ArticleGraphOptions;

const jsonLd = articleGraph(Astro.props);
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
<slot />
