---
import { breadcrumbGraph } from '@getvitops/utils';
import type { BreadcrumbGraphOptions, BreadcrumbItem } from '@getvitops/utils';

export type { BreadcrumbItem };
export type Props = BreadcrumbGraphOptions;

const jsonLd = breadcrumbGraph(Astro.props);
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
<slot />
