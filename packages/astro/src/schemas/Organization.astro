---
import { organizationGraph } from '@getvitops/utils';
import type { OrganizationGraphOptions } from '@getvitops/utils';

export type Props = OrganizationGraphOptions;

const jsonLd = organizationGraph(Astro.props);
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
<slot />
